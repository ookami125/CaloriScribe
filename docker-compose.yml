version: "3.9"

services:
  nutrition-tracker:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: "file:./data/nutrition.db"
    volumes:
      - nutrition-data:/app/data
    restart: unless-stopped

volumes:
  nutrition-data:
